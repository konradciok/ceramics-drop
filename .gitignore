# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
.open-next/
.wrangler/
.dev.vars

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files
.env
.env*.local

# Notion i18n sync state (local page IDs + timestamps)
.notion-i18n/
.env.sentry-build-plugin

# local google credentials
.secrets/
gtm-oauth-client.json
docs/gtm-api.json
*-service-account*.json

# ad-hoc debug/log exports — never commit (may contain tokens/DSN keys in headers)
docs/logs-*.json

# playwright
/test-results/
/playwright-report/
/.playwright-mcp/
/e2e/REPORT.md

# typescript
*.tsbuildinfo
next-env.d.ts
worker-configuration.d.ts

# editor
.vscode/*
!.vscode/extensions.json
.idea
design/

# Claude Code: settings.json is shared (tracked); everything else stays local
.claude/*
!.claude/settings.json
.worktrees/

# local Pi agent config (per-machine)
.pi/

# Brainstorming visual companion (local only)
.superpowers/
1Password Emergency Kit.pdf
supabase/.temp/

# locally installed agent skills (per-machine; the tracked skills-lock.json
# records what to reinstall)
.agents/

# autogenerated Codex per-environment config
.codex/environments/

# scratch PR-review notes/diffs (throwaway, not repo docs)
pr-review/

# ad-hoc order/customer CSV exports — contain PII, never commit
/exports/
.dev.vars_BACK
.env.local_BACK

# staging area for files pending manual deletion (sandboxed tools can move but
# not delete) — empty it whenever you like
/_to_delete/
prompt.md
docs/workers-logs-14.08.2026.json
docs/archive/image.png
