# LAN reverse proxy for the local Studio Admin (Next.js on the host).
# Upstream: host.docker.internal:3010 (npm run admin:dev:lan)

server {
    listen 80;
    server_name
        studio-admin.home.arpa
        studio-admin.local
        studio-admin-konradciok.local;

    client_max_body_size 4m;

    location / {
        proxy_pass http://host.docker.internal:3010;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_read_timeout 300s;
    }
}
